#include <stdlib.h>

int ft_strlen(const char *s) {
  int i;

  i = 0;
  while (s[i] != '\0') {
    i++;
  }
  return (i);
}

int ft_memcmp(const *s1, const *s2, size_t n)
{
    if (n == 0)
        return (0);
    while ((*s1 != '\0' || *s2 != '\0') && n > 0)
    {
      
    }
}